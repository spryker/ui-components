export enum ButtonSize {
  Large = 'lg',
  Medium = 'md',
  Small = 'sm',
}

export enum ButtonType {
  Button = 'button',
  Submit = 'submit',
}

export type ButtonAttributes = Record<string, string>;
