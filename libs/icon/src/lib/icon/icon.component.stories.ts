import { IconComponent } from './icon.component';

export default {
  title: 'IconComponent',
};

export const primary = () => ({
  moduleMetadata: {
    imports: [],
  },
  component: IconComponent,
  props: {},
});
